#include "lists.h"
#include <string.h>
#include <stdlib.h>

int _strlen(char *s)
{
    int i, len = 0;
    for (i = 0; s[i] != '\0'; i++)
    {
        len++;
    }
    return len;
}

list_t *add_node(list_t **head, const char *str)
{
    // Rest of the code
}
